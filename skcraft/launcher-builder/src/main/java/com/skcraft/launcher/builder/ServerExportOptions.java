// Generated by delombok at Sat Jul 14 01:46:55 CEST 2018
/*
 * SK's Minecraft Launcher
 * Copyright (C) 2010-2014 Albert Pham <http://www.sk89q.com> and contributors
 * Please see LICENSE.txt for license information.
 */
package com.skcraft.launcher.builder;

import com.beust.jcommander.Parameter;
import java.io.File;

public class ServerExportOptions {
    @Parameter(names = "--source", required = true)
    private File sourceDir;
    @Parameter(names = "--dest", required = true)
    private File destDir;

    @SuppressWarnings("all")
    public ServerExportOptions() {
    }

    @SuppressWarnings("all")
    public File getSourceDir() {
        return this.sourceDir;
    }

    @SuppressWarnings("all")
    public File getDestDir() {
        return this.destDir;
    }

    @SuppressWarnings("all")
    public void setSourceDir(final File sourceDir) {
        this.sourceDir = sourceDir;
    }

    @SuppressWarnings("all")
    public void setDestDir(final File destDir) {
        this.destDir = destDir;
    }

    @Override
    @SuppressWarnings("all")
    public boolean equals(final Object o) {
        if (o == this) return true;
        if (!(o instanceof ServerExportOptions)) return false;
        final ServerExportOptions other = (ServerExportOptions) o;
        if (!other.canEqual((Object) this)) return false;
        final Object this$sourceDir = this.getSourceDir();
        final Object other$sourceDir = other.getSourceDir();
        if (this$sourceDir == null ? other$sourceDir != null : !this$sourceDir.equals(other$sourceDir)) return false;
        final Object this$destDir = this.getDestDir();
        final Object other$destDir = other.getDestDir();
        if (this$destDir == null ? other$destDir != null : !this$destDir.equals(other$destDir)) return false;
        return true;
    }

    @SuppressWarnings("all")
    protected boolean canEqual(final Object other) {
        return other instanceof ServerExportOptions;
    }

    @Override
    @SuppressWarnings("all")
    public int hashCode() {
        final int PRIME = 59;
        int result = 1;
        final Object $sourceDir = this.getSourceDir();
        result = result * PRIME + ($sourceDir == null ? 43 : $sourceDir.hashCode());
        final Object $destDir = this.getDestDir();
        result = result * PRIME + ($destDir == null ? 43 : $destDir.hashCode());
        return result;
    }

    @Override
    @SuppressWarnings("all")
    public String toString() {
        return "ServerExportOptions(sourceDir=" + this.getSourceDir() + ", destDir=" + this.getDestDir() + ")";
    }
}
